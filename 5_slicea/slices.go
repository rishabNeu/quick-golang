package main

import "fmt"

// slice is dynamic array
func main() {
	// same as array, but without size
	// uninitialized slice is nil\
	var nums []int
	// fmt.Println(nums == nil)
	fmt.Println(len(nums))
 
}
